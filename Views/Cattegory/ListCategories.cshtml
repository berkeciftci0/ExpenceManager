@model ExpenceManager.Models.Entity.TBLKATEGORILER
@{
    ViewBag.Title = "ListCategories";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2>ListCategories</h2>

@using (Html.BeginForm("Guncelle", "Cattegory", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(m => m.KATEGORIID)
        @Html.TextBoxFor(m => m.KATEGORIID, new { @class = "form-control" })
        <br />
        @Html.LabelFor(m => m.KATEGORIAD)
        @Html.TextBoxFor(m => m.KATEGORIAD, new { @class = "form-control" })
    </div>
    <div>
        <button class="btn btn-warning">Cattegory Update</button>
    </div>
} 